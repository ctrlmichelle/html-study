OOEN A TERMINAL AND TYPE GIT. Its good if you get 'git help-a' its good.

git init = makes the folder start working with git

git add . = tells git to track all files

git commit-m " a message" - attach a message to changes done

git config global user.mail "email used in git"

git config global username "githubcom username"

repeat git commit...

git remote add origin https://github.com/ctrlmichelle/html-study.git - connect folder in your comp to repository

git push origin master - pushes code from your computer to git hub repository

- a pop up appears
- sign in in browser (takes you to the browser)
- authorize ( green button) or sign in...
- chech if TERMINAL is the same as trainer
- go to github and refresh
- send link and byee 
when  youve done it before: git add . and the git commit -m lastly git push origin master
